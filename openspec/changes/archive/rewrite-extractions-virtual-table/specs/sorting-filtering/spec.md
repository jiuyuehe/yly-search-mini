# Capability: Sorting & Filtering (Archived)

## ADDED Requirements
- Requirement: Sort by time and id via toolbar controls; default by time desc.
  - Scenario: Toggling sort applies immediately to current dataset.
- Requirement: Keyword filter matches any flattened field value (case-insensitive) and updates display in real time.
  - Scenario: Clearing filter restores full dataset.
