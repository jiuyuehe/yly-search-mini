# Capability: Navigation Link (Archived)

## ADDED Requirements
- Requirement: Include esId column with clickable link per row.
  - Scenario: Clicking esId navigates to `/preview/doc/<esId>?retureBtn=true` in SPA hash routing.
